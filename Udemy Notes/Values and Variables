# Values and Variables

## Vocabulary

* string = textual information
* number = numeric value
* Boolean = True or false value
* Modulo = returns the remainder of a divison ex. 25 % 3 = 1; 14 % 4 = 2
* cubed = "** ex. 2 **" 3=8
* NAN = not a number, a numeric value that represents something that is not a number ex. 1 + Nan = NAN
* ++ and -- will increment or decrement. Ex. let = people; people++ = 1 people ++ = 2
* Properties = values assgined to strings ex. 'hello'.length = 5
* indicies =  let = mysong[1] this will = 'y'
* methods are built in actoins in a special value
* String examples;  stringOfSomeSort.method(); str.toUpperCase(); 
* let color = '        purple';  color.trim() = purple; color.trim toUpperCase = PURPLE
* items within the () are called arguments 
* 'baseball'.indexOf('ball') = 4
* if index of (place item here) = -1 then item is not here
* slice = the ability to grab a portion of a string ex. if you wanted to remove the $ from a shopping list you could write '$50.60.slice(1)
* replace = replaces a specific string with what you wanted ex. 'baseball is entertaining'.replace('entertaining', 'ok')
* escape notation ex. /n = NEW LINE 'hello I ain/'t'; the "/" will hide the ' and not close the quotation. 
* interpelate; back ticks; let animal = 'pig' let sound = 'oink' `{animal} says ${sound}` "pig says oink"
* Ex. for back ticks; let item = 'cucumbers'; let price = 1.99; let quantity = 4; `you bought ${quantity} ${item}, total price: $${price*quantity}`
// you bought 4 cucumbers, total price $7.96
* Math object; math.pow (7,2) = 42 math.floor(4.6) = 5 Math.floor(3.657) = 3
* Random numbers; Math.random(); random number from zero to one. 
 ### Ex. How to get random numbers of other values like 1-10. 
1. const step1 = Math.random();
2.  Const step2 = step1 * 10 const 
3. step3 = Math.floor(step2); 
4. const step4 = step3 + 1;
5. Math.floor(Math.random()*10)+1
### Parse int and Parse float are used to parse strings into numbers.
1. parseInt('21') + 1 = 22
2. parseFloat ('33.5') = 33.5
* Nesting = having if and else if statements within themselves
### Truthy and Falsy
* all numbers except for ZERO are Truthy everything besides the list below is Truthy
#### Falsy Values 
1. false
2. 0
3. empty string ''
4. null
5. undefined
6.Nan
 